<p style="color: green"><%= notice %></p>
<style>
         table, th, td {
            border: 1px solid black;
         }
</style>
<%= render @book_requests %>
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Title</th>
    
        <th>Author</th>
      
        <th>Description</th>
        
        <th>Status</td>
      </tr>
    </thead>

    <tbody>
     <% @book_requests.each do |book_request| %>
        <tr>
          <td ><%= book_request.user.email %></td>
          <td><%= book_request.book.title %></td>
          <td><%= book_request.book.author %></td>
          <td><%= book_request.status %></td>
          <td>
            <% if book_request.status.eql?("pending") %>
            <%= button_to "Cancle", book_request, method: :delete %> </td>
            <%end%>
        </tr>
        <% end %>
      </tbody>
    </table>
<div>
  <%= link_to "Back to book List ", books_path %>
</div>
